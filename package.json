{
  "name": "debug_server",
  "version": "1.0.0",
  "description": "Debug your Unity XR projects via the network.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx electron-packager . \"Debug Server\" --platform=win32 --out=build && cd build && node ../exe"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "electron-store": "^8.0.2",
    "express": "^4.18.1",
    "ngrok": "^4.3.1"
  },
  "devDependencies": {
    "@types/body-parser": "^1.19.2",
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "electron": "^19.0.4",
    "electron-winstaller": "^5.0.0",
    "exit-hook": "^3.0.0"
  }
}
